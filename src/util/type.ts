import { IconType } from 'react-icons';

export interface ServicesProps {
  title: string;
  about: string;
  Icon: IconType;
}

export interface SkillProps {
  name: string;
  level: string;
  Icon: IconType;
}
